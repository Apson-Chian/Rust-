# rkv 多人协作方案

## 1. 当前基线

当前项目约 1,800 行，只有标准库依赖，已经实现协议、内存存储、追加日志、
执行引擎、TCP 服务端、命令行客户端以及单元/集成测试。

基线检查结果：

- `cargo fmt --all -- --check` 通过；
- `cargo clippy --all-targets -- -D warnings` 通过；
- 23 个单元测试、8 个集成测试全部通过；
- TTL 溢出、空请求无响应、响应解析过宽、恢复数据缺少校验、
  测试服务器无法受控退出等 P0 问题已完成修复；
- 持久化崩溃一致性、连接资源限制和慢磁盘 I/O 串行化等 P1 边界仍需继续完善。

已有提交历史保留不动。下面的任务通过真实的个人分支、提交和 Pull Request
继续开发，以代码评审和测试记录体现后续协作。

## 2. 四人默认分工

| 成员 | 负责领域 | 主要文件 | 第一阶段交付 |
| --- | --- | --- | --- |
| A：协议与客户端 | 命令/响应模型、输入校验、客户端体验、参数解析 | `src/protocol.rs`、`src/config.rs`、`src/bin/client.rs`、`src/error.rs` | 明确协议边界；严格解析响应；补齐空白、最大长度和畸形响应测试 |
| B：存储与持久化 | 内存数据、TTL、日志编码/恢复/压缩、崩溃一致性 | `src/store.rs`、`src/persist.rs` | 校验恢复数据；完善追加失败和压缩失败处理；补齐恢复测试 |
| C：引擎与网络服务 | 命令执行、并发、连接生命周期、服务端入口 | `src/engine.rs`、`src/server.rs`、`src/bin/server.rs` | 修复 TTL 算术溢出；处理锁中毒；增加可控停机和连接资源限制 |
| D：测试、质量与文档 | 集成测试框架、端到端场景、CI、使用文档 | `tests/`、`README.md`、CI 配置 | 重构测试服务器生命周期；增加异常/并发/重启场景；建立质量门禁 |

协调人由一名成员兼任，只负责接口确认、版本合并和发布，不代替其他成员提交代码。

### 人数调整

- 3 人：A 保持不变；B 负责存储与持久化；C 合并“引擎与网络”和“测试、质量与文档”。
- 5 人：把 B 拆成“内存存储与 TTL”和“日志持久化”；其余三组不变。

## 3. 已发现问题与任务归属

### P0：本轮已完成

- [x] A：保留 `SET`/`SETEX` 值尾部空格，并让客户端、解析和编码行为一致。
- [x] A：`Response::parse` 遇到缺失、重复或非法 `STATS` 字段时会报错，
  `OK`、`PONG`、`BYE` 等响应不再静默接受多余参数。
- [x] C：使用受检算术计算 `SETEX` 过期时间，避免极大 TTL 导致线程 panic、
  `Engine` 互斥锁中毒以及后续客户端不可用。
- [x] C：空请求返回协议错误，不再让等待“一问一答”的客户端永久阻塞。
- [x] B：日志恢复时重新校验键和值，拒绝会破坏网络协议的数据。
- [x] D：测试服务支持显式关闭与线程回收；“重启恢复”测试会先确认旧服务已退出，
  再使用同一数据文件启动新服务。

### P1：可靠性完善

1. B：评估部分写入、`sync_data` 失败以及压缩重命名后的恢复策略；
   对项目宣称的持久性边界给出准确文档和故障测试。
2. B：压缩成功后同步父目录（在支持的平台），并明确跨平台重命名行为。
3. C：限制同时连接/线程数量，增加读写超时，避免大量空闲连接耗尽资源。
4. C：减少持有全局 `Mutex<Engine>` 时执行慢磁盘 I/O 对其他客户端的影响，
   或在文档中明确当前串行写模型。
5. D：增加日志故障注入、连接资源上限和更长时间的高并发压力测试。

### P2：展示与扩展

1. D：增加 GitHub Actions，固定执行格式检查、Clippy 和全部测试。
2. A：改善客户端历史记录、批处理输入或更清晰的帮助输出。
3. C：增加优雅停机和结构化运行日志。
4. B：提供日志统计信息，为自动压缩阈值打基础。

## 4. 分支与合并规则

- `main`：只保存可演示、全部检查通过的版本，不直接提交功能代码。
- `develop`：阶段集成分支。
- 个人任务分支：`feature/protocol-client`、`feature/storage-persist`、
  `feature/server-engine`、`test/qa-docs`。
- 每个 Pull Request 只解决一个主题；标题建议使用
  `模块: 动词 + 目标`，例如 `engine: 拒绝溢出的 TTL`。
- 至少由一名非作者评审；跨模块接口改动同时邀请上下游负责人。
- 合并前必须通过：

  ```bash
  cargo fmt --all -- --check
  cargo clippy --all-targets -- -D warnings
  cargo test
  ```

- 不把格式化、重命名和功能修改混在同一提交，降低冲突和评审难度。

## 5. 接口边界

依赖方向保持为：

```text
bin/client ──> protocol <── server <── bin/server
                              │
                              v
                           engine
                           /    \
                        store  persist
                           \    /
                            error
```

- A 维护 `Command`、`Response` 和协议常量；修改公共枚举前先发接口 PR。
- B 维护 `Store`、`Entry`、`Record` 和 `AppendLog`，不直接处理网络请求。
- C 只通过公开接口组合协议、存储和持久化，不复制解析或日志逻辑。
- D 优先从公共 API 和真实 TCP 行为测试，不依赖私有实现细节。
- `src/lib.rs` 的公共导出由协调人或相关双方共同修改。

## 6. 推荐实施顺序

1. D 先提交可控停机的测试夹具，但暂不改变业务行为。
2. A、B、C 基于同一个 `develop` 基线并行完成各自 P0；D 同步补失败测试。
3. 按 A（协议）→ B（持久化）→ C（服务集成）→ D（端到端验收）的顺序合并。
4. 全员进行一次交叉评审：A 审 C，C 审 B，B 审 D，D 审 A。
5. P0 全部通过后打阶段标签，再并行处理 P1。

## 7. 完成定义

一个任务只有同时满足以下条件才算完成：

- 有对应 Issue、个人分支和可审阅的 Pull Request；
- 新行为有单元测试或集成测试，修复类任务先能复现旧问题；
- 格式检查、Clippy、全部测试通过；
- 公共接口和用户可见行为已更新文档；
- 至少一名非作者完成评审，提出的问题已处理；
- 合并后 `develop` 可构建、可启动、可由客户端完成 CRUD 演示。
